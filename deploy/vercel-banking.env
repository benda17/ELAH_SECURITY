# ELAH Banking System — Vercel environment variables
# Import into Vercel → Settings → Environment Variables (Production + Preview).
# Then redeploy ELAH_BANKING_SYSTEM.
#
# Set DATABASE_URL to your Neon connection string (do not use file:./dev.db).

# DATABASE_URL=postgresql://USER:PASSWORD@ep-XXXX.neon.tech/neondb?sslmode=require
AUTH_SECRET=OS9SsEuw+mHOi5AT8D0idmbmmyUIesbSF4aCTxhWi1hWKMwE2629RZIRAqcdSn8l
LOG_MIRROR_JSONL=false
OPENAI_MODEL=gpt-4o-mini

# Optional:
# OPENAI_API_KEY=sk-proj-your-key-here

# Phase 3 ELAH scoring (founder pastes the live token; do not commit secrets).
# Unset ELAH_SERVICE_URL = in-process colocated scorer (ELAH-ARCH-OWN-001).
# ELAH_SERVICE_TOKEN=
# ELAH_SERVICE_URL=
# ELAH_SCORE_TIMEOUT_MS=250
