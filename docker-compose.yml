services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: elah
      POSTGRES_PASSWORD: elah
      POSTGRES_DB: elah_banking
    volumes:
      - elah_pg:/var/lib/postgresql/data

volumes:
  elah_pg:
